#include<bits/stdc++.h>

using namespace std;
const int N = (int)1e5 + 100;

string s[N];
int dp1[N], dp2[N];

const int inf = 1 << 30;

int main(){
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif

    int n;
    cin >> n;
    
    for (int i = 0; i < n;i++){
        cin >> s[i];
    }
    for (int i = 0; i < n;i++){
        dp1[i] = inf;
        dp2[i] = inf;
        if()
    }
        return 0;
}